# Supabase project settings -> API
# Until both are set, the app runs in preview mode: the board renders, the
# sign-in guard stands down, and no Supabase client is constructed.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
