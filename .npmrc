# @neondatabase/auth declares peer next>=16, but this project is pinned to
# Next 14 and the SDK builds and runs against it (see docs/neon-auth-notes.md).
# Without this flag every `npm install` / `npm ci` fails on ERESOLVE.
legacy-peer-deps=true
